version: "3.9"

services:
  mysql:
    image: mysql:8.0
    container_name: info-kepegawaian-mysql
    restart: unless-stopped
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root123}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-sisdmk2}
      MYSQL_USER: ${MYSQL_USER:-pegawai}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-pegawai123}
      TZ: ${TZ:-Asia/Jakarta}
    ports:
      - "${MYSQL_PORT:-3306}:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./backend/sql:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-p${MYSQL_ROOT_PASSWORD:-root123}"]
      interval: 10s
      timeout: 5s
      retries: 20

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: info-kepegawaian-backend
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT: 3000
      TZ: ${TZ:-Asia/Jakarta}
      DATABASE_URL: mysql://${MYSQL_USER:-pegawai}:${MYSQL_PASSWORD:-pegawai123}@mysql:3306/${MYSQL_DATABASE:-sisdmk2}
      DB_HOST: mysql
      DB_PORT: 3306
      DB_USER: ${MYSQL_USER:-pegawai}
      DB_PASS: ${MYSQL_PASSWORD:-pegawai123}
      DB_NAME: ${MYSQL_DATABASE:-sisdmk2}
      JWT_SECRET: ${JWT_SECRET:-ganti-jwt-secret-anda}
      API_URL: ${API_URL:-http://localhost:3000}
    ports:
      - "${BACKEND_PORT:-3000}:3000"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:3000}
    container_name: info-kepegawaian-frontend
    restart: unless-stopped
    depends_on:
      - backend
    environment:
      NODE_ENV: production
      PORT: 3004
      TZ: ${TZ:-Asia/Jakarta}
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:3000}
    ports:
      - "${FRONTEND_PORT:-3004}:3004"

volumes:
  mysql_data:
